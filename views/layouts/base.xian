{{> head}}
<body class="h-full font-[Poppins] bg-neutral-bg text-gray-800">
  <div class="flex min-h-screen w-full">
    {{> sidebar activeRoute=activeRoute year=year }}
    <div class="flex-1 flex flex-col min-w-0">
      {{> topbar pageTitle=title breadcrumbs=breadcrumbs}}
      <main class="p-6 space-y-6">
        {{> alerts}}
        {{> breadcrumbs items=breadcrumbs}}
        {{{ body }}}
      </main>
      {{> footer}}
    </div>
  </div>
</body>
</html>